package application;

import java.util.Scanner;

import entities.ExProp;

public class exPropostos {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		
		
		// ATIVIADE 1)
		/*
		int n;
		
		do {
		System.out.print("Quantos numeros vc vai digitar (max 10): ");
		 n = sc.nextInt();
		
		if( n > 10) {
			System.out.println("\nValor excedido!\n");
		}	
	}while ( n > 10 );
		
		int[] vect = new int[n];
		
		for (int i=0;i<vect.length;i++) {
			System.out.print("Digite o numero: ");
			vect[i] = sc.nextInt();
		}
		
		System.out.println("\nNumeros negativos:");
		
		for (int i=0;i<vect.length;i++) {
			if(vect[i]< 0) {
				System.out.println(vect[i]);
			}
		}
		
		ATIVIADE 2)
		System.out.print("Quantos numeros voce vai digitar? ");
		int n1 = sc.nextInt();
		
		double[] vect1 = new double[n1];
		for (int i=0;i<vect1.length;i++) {
			System.out.print("Digite o numero: ");
			vect1[i] = sc.nextInt();
		}
		
		System.out.print("\nValores = ");
		for (int i=0;i<vect1.length;i++) {
			System.out.printf("%.2f  ", vect1[i]);
		}
		
		double soma = 0;
		for (int i=0;i<vect1.length;i++) {
			soma += vect1[i];
		}
		System.out.printf("\nSoma = %.2f", soma);
		
		double media = soma / vect1.length;
		System.out.printf("\nMedia = %.2f",media);
		
		*/
	
		
		
		
		
		
		
		sc.close();

	}

}
